import styles from './styles.module.css';

const MapIndicatorModal = () => {
  return (
    <div className={styles.mapIndicatorModal}>
      <h3>Map Indicator Modal</h3>
    </div>
  );
};

export default MapIndicatorModal;
